export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Automated Form Filler</h1>
      <p>
        Backend endpoints are ready at <code>/api/process</code> and
        <code>/api/generate</code>.
      </p>
    </main>
  );
}
